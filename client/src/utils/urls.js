import {
  BACKEND_URL
} from '../config';

// states
export const GET_STATES = `${BACKEND_URL}/state_list`;

// districts
export const GET_DISTRICTS = `${BACKEND_URL}/district_list`;

// assembly
export const GET_ASSEMBLY = `${BACKEND_URL}/assembly_list`;

// captcha
export const GET_CAPTCHA = `${BACKEND_URL}/captcha`;

//
export const GET_TREE = `${BACKEND_URL}/tree`;
export const GET_TREES = `${BACKEND_URL}/trees`;
